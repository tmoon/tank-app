const terra = require('@terra-money/core');

function create_new_user(pin) {

    // returns pub key, encrypted mnemonic
}

function recover_user(mnemonic, pin) {
    
    return encrypted_mnemonic, address
}